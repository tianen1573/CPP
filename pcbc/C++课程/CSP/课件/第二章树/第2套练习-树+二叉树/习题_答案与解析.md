# 🌲 树 · 综合练习 · 答案与解析（第2套）

> **知识点：** 树的基本概念 · 二叉树性质 · 满/完全二叉树 · 遍历与还原 · 树的形态
> **参考：** CSP-J / GESP 初赛

---

## 一、选择题答案与解析

**1.** 答案 **B**

| 选项 | 分析 |
|------|------|
| A | ❌ 50 是把节点数当成边数 |
| B ✅ | 树的性质：边数 = 节点数 - 1 = 50 - 1 = 49 |
| C | ❌ 51 是节点数 + 1 |
| D | ❌ 48 是节点数 - 2 |

> 考点：树的最基本性质（边数 = 节点数 - 1）。真题溯源。

---

**2.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | 前序 `ABDCEF` 定根 A；中序 `DBAECF` 分左右：左子树 {D,B}、右子树 {E,C,F}。递归还原：B 左 D，C 左 E 右 F。后序 = 左子树后序(D B) + 右子树后序(E F C) + 根(A) = **D B E F C A** |
| B | ❌ 这是中序遍历结果 D B E A F C 的变形 |
| C | ❌ E、B 顺序错（右子树的后序应为 E F C） |
| D | ❌ 这是前序遍历结果 |

> 考点：已知前序 + 中序还原二叉树，再求后序。真题溯源（每年必考）。

---

**3.** 答案 **C**

| 选项 | 分析 |
|------|------|
| A | ❌ 10 是误把"度为 2 的节点数"当叶子数 |
| B | ❌ 11 是少算了 1（数错） |
| C ✅ | 边数 = 5×2 + 3×3 + 4×1 = 23，节点数 = 23 + 1 = 24；叶子 = 24 - 5 - 3 - 4 = **12** |
| D | ❌ 13 是把边数当成节点数再减 |

> 考点：度与边数的关系（总边数 = Σ度），由边数推节点数。真题溯源。

---

**4.** 答案 **B**

| 选项 | 分析 |
|------|------|
| A | ❌ 3 是漏算了"左 2 右 0 / 左 0 右 2"两种分布 |
| B ✅ | 3 个节点的二叉树形态：左子树 2 个（2 种）+ 右 0（1 种）、左 0 + 右 2（2 种）、左 1 + 右 1（1 种），共 2+2+1 = **5** 种 |
| C | ❌ 6 是 3! 的误算 |
| D | ❌ 4 是漏算了一种左右分布 |

> 考点：二叉树的形态计数（卡特兰数：1、2、5、14…）。陷阱：左右子树区分，n=3 时是 5 不是 4。

---

**5.** 答案 **B**

| 选项 | 分析 |
|------|------|
| A | ❌ 64 = 2^6，是第 7 层的节点数上限（2^(7-1)） |
| B ✅ | 深度 k 的二叉树最多 2^k - 1 个节点：2^7 - 1 = **127** |
| C | ❌ 128 = 2^7，忘了减 1 |
| D | ❌ 63 = 2^6 - 1，深度算成了 6 |

> 考点：二叉树性质②（深度 k 最多 2^k - 1 个节点）。陷阱：与"第 k 层最多 2^(k-1) 个"混淆。

---

**6.** 答案 **B**

| 选项 | 分析 |
|------|------|
| A | ❌ 编号 4 的右孩子是 2×4+1 = 9，不是 8；8 是 4 的**左**孩子 |
| B ✅ | n = 15，⌊15/2⌋ = 7，编号 > 7 的全是叶子，8 > 7 → 是叶子节点 |
| C | ❌ 编号 8 的左孩子 16 > 15 不存在，更没有右孩子 |
| D | ❌ 根节点（编号 1）的左孩子是编号 2 |

> 考点：完全二叉树编号规律（左孩子 2i、右孩子 2i+1、编号 > ⌊n/2⌋ 为叶子）。真题溯源。

---

**7.** 答案 **C**

| 选项 | 分析 |
|------|------|
| A | ❌ 9 是把 25 - 4 - 12 算错（n₂ 应为 10） |
| B | ❌ 10 是度为 2 的节点数，不是叶子数 |
| C ✅ | n = n₀ + n₁ + n₂，且 n₀ = n₂ + 1：25 = (n₂+1) + 4 + n₂ → n₂ = 10 → n₀ = **11** |
| D | ❌ 12 是叶子 + 度1 的误算 |

> 考点：n₀ = n₂ + 1 的灵活应用（联立方程）。真题溯源。

---

**8.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | n₀ = n₂ + 1 = 12 + 1 = 13，总节点数 = n₀ + n₁ + n₂ = 13 + 5 + 12 = **30** |
| B | ❌ 28 是漏加度为 1 的节点数 |
| C | ❌ 29 是 30 - 1 的误算 |
| D | ❌ 31 是把度为 2 的节点当成了 13 个 |

> 考点：n₀ = n₂ + 1 联立求总节点数。陷阱：记得加上 n₁。

---

**9.** 答案 **C**

| 选项 | 分析 |
|------|------|
| A | ✅ 正确：第 i 层最多 2^(i-1) 个 |
| B | ✅ 正确：二叉树是**有序**树，左右子树顺序不能交换 |
| C ❌ | **错误**：深度为 k 的二叉树最多 2^k **- 1** 个节点，不是 2^k 个（少减了 1） |
| D | ✅ 正确：n₀ = n₂ + 1 |

> 考点：二叉树四大性质的辨析。陷阱：2^k 与 2^k - 1 差 1。

---

**10.** 答案 **C**

| 选项 | 分析 |
|------|------|
| A | ❌ 49 是 ⌊100/2⌋ - 1 或按"一半减一"误算 |
| B | ❌ 51 是 100 - 49（把非叶子算成 49） |
| C ✅ | 完全二叉树叶子数 = ⌈n/2⌉ = ⌈100/2⌉ = **50** |
| D | ❌ 48 是按 96/2 误算 |

> 考点：完全二叉树叶子数公式 ⌈n/2⌉。真题溯源。

---

**11.** 答案 **B**

| 选项 | 分析 |
|------|------|
| A | ❌ `A B D E F C` 中 F 的位置错（C 的左孩子是 F，右孩子为空） |
| B ✅ | 后序 `DEBFCA` 定根 A；中序 `DBEAFC` 分左右：左 {D,B,E}、右 {F,C}。递归：左子树根 B（左 D 右 E），右子树根 C（左 F）。前序 = **A B D E C F** |
| C | ❌ `A B C D E F` 把中序当层序了 |
| D | ❌ `A D B E C F` 是左子树内部顺序错 |

> 考点：已知后序 + 中序还原二叉树，求前序。真题溯源。

---

**12.** 答案 **C**

| 选项 | 分析 |
|------|------|
| A | ❌ 2 是 7 个节点排成 4 层以上？深度 2 最多 3 个节点，装不下 7 个 |
| B | ❌ 4 是可以的深度，但不是"最小" |
| C ✅ | 深度最小 = 尽量排满每层：2³ - 1 = 7，正好 7 个节点 → 深度 **3**（⌊log₂7⌋+1 = 3） |
| D | ❌ 5 是链状时的深度，不是最小 |

> 考点：深度最小 ⟺ 尽量满（完全二叉树形态）。陷阱：最小 vs 最大深度。

---

**13.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ❌ 3 是第 2 层（编号 2–3）的误判 |
| B | ❌ 5 是按 2^4 = 16 > 10 数错 |
| C | ❌ 6 是按 2^5 = 32 数错 |
| D ✅ | 第 k 层编号范围 [2^(k-1), 2^k - 1]：第 4 层编号 8–15，10 在其中 → 第 **4** 层（⌊log₂10⌋+1 = 4） |

> 考点：完全二叉树编号与层数的关系。陷阱：第 k 层编号从 2^(k-1) 开始，不是 2^(k-1)+1。

---

**14.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | 深度 5 的完全二叉树：前 4 层必须满（1+2+4+8 = 15），第 5 层至少 1 个 → 最少 **16** 个 |
| B | ❌ 15 是深度 4 的满二叉树节点数 |
| C | ❌ 17 是第 5 层放 2 个的情况，不是"最少" |
| D | ❌ 31 是深度 5 的满二叉树（最多）节点数 |

> 考点：完全二叉树节点数范围 [2^(k-1), 2^k - 1]。陷阱：最少 vs 最多、深度 4 满 vs 深度 5 满。

---

**15.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ❌ 14 是 8 + 6 的误算（只算了 n₀ + n₁） |
| B | ❌ 16 是 2 × 8 的误算 |
| C | ❌ 17 是 8 + 8 + 1 的误算 |
| D ✅ | 无度为 1 的节点 → n = n₀ + n₂ = n₀ + (n₀ - 1) = 8 + 7 = **15** |

> 考点：n₀ = n₂ + 1 在"无度1节点"条件下的应用。陷阱：总节点 = n₀ + n₁ + n₂。

---

**16.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | 满二叉树所有非叶子节点都有两个孩子，前序 + 后序可以唯一确定 |
| B | ❌ 全左链：前序 + 后序无法区分左右 |
| C | ❌ 完全二叉树可能含单分支节点，仍无法唯一确定 |
| D | ❌ 深度 3 不代表所有节点都有两个孩子 |

> 考点：两种遍历还原二叉树的充分条件。陷阱：前序 + 后序通常不行，只有"所有非叶子都有两个孩子"（如满二叉树）才行。

---

**17.** 答案 **C**

| 选项 | 分析 |
|------|------|
| A | ❌ 31 是深度 5 满二叉树的总节点数 2^5 - 1 |
| B | ❌ 15 是第 4 层的节点数 |
| C ✅ | 满二叉树叶子数 = 2^(k-1) = 2^4 = **16**（全在最底层） |
| D | ❌ 32 = 2^5，把总节点数当叶子数 |

> 考点：满二叉树性质（叶子数 2^(k-1)）。陷阱：总节点数 2^k - 1 与叶子数 2^(k-1) 混淆。

---

**18.** 答案 **B**

| 选项 | 分析 |
|------|------|
| A | ❌ 前序最后输出的是最右下的节点 |
| B ✅ | 后序 = 左 → 右 → 根，**最后一个一定是根节点** |
| C | ❌ 中序最后输出的是最右节点 |
| D | ❌ 层序最后输出的是最后一层最右的节点 |

> 考点：遍历顺序与根的位置（前序首 = 根、后序末 = 根）。陷阱：层序第一个是根但最后一个不是。

---

**19.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ❌ 2i 是左孩子编号 |
| B | ❌ 2i + 1 是右孩子编号 |
| C | ❌ i - 1 是顺序存储相邻下标，不是父节点 |
| D ✅ | 完全二叉树编号 i 的父节点 = **⌊i / 2⌋** |

> 考点：完全二叉树编号规律（父 ⌊i/2⌋）。真题溯源。

---

**20.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | 后序 `BDCAFGE` 定根 E；中序 `ABCDEFG` 分左右：左 {A,B,C,D}、右 {F,G}。左子树：后序 BDCA 定根 A，中序 ABCD → A 只有右孩子 C（C 左 B 右 D）；右子树：后序 FG 定根 G，G 左 F。前序 = **E A C B D G F** |
| B | ❌ 右子树前序应为 G F，不是 F G |
| C | ❌ 左子树结构错（A 的右孩子是 C 不是 B） |
| D | ❌ 根应为 E |

> 考点：已知中序 + 后序还原二叉树。真题溯源。

---

**21.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | 树中任意两个节点之间有且仅有一条路径（连通且无环） |
| B | ❌ 树有 n 个节点 → n - 1 条边 |
| C | ❌ 树的度是所有节点中的**最大度**，不是叶子个数 |
| D | ❌ 二叉树是**有序**树且节点最多两个孩子；"度为 2 的树"不区分左右，两者不同 |

> 考点：树的基本概念辨析。陷阱：二叉树 ≠ 度为 2 的树（有序性 + 允许度 < 2）。

---

**22.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ❌ 31 是深度 5 满二叉树的总节点数 |
| B | ❌ 30 是 31 - 1 的误算 |
| C | ❌ 33 是 32 + 1 的误算 |
| D ✅ | 2^k - 1 = 63 → k = 6，叶子数 = 2^(k-1) = 2^5 = **32** |

> 考点：满二叉树公式逆推（由总节点数求深度，再求叶子数）。陷阱：总节点数 2^k - 1 与叶子数 2^(k-1) 混淆。

---

**23.** 答案 **C**

| 选项 | 分析 |
|------|------|
| A | ❌ 12 是左孩子编号（2×6） |
| B | ❌ 11 是编号 5 的右孩子 |
| C ✅ | 右孩子 = 2i + 1 = 2×6 + 1 = **13**，且 13 ≤ 15 存在 |
| D | ❌ 13 ≤ n = 15，右孩子存在 |

> 考点：完全二叉树编号规律（右孩子 2i+1，需判断是否越界）。真题溯源。

---

**24.** 答案 **B**

| 选项 | 分析 |
|------|------|
| A | ❌ 8 是 5 + 3 的误算（把 n₂ 算成 3） |
| B ✅ | n₀ = n₂ + 1 → n₂ = 5 - 1 = 4；n = n₀ + n₂ = 5 + 4 = **9** |
| C | ❌ 10 是 2 × 5 的误算 |
| D | ❌ 11 是 5 + 5 + 1 的误算 |

> 考点：n₀ = n₂ + 1 的逆向应用（已知叶子求节点数）。陷阱：无度1节点时 n = n₀ + n₂。

---

**25.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ❌ 6 = ⌊log₂100⌋，忘了 +1 |
| B | ❌ 8 是 2^7 = 128 > 100 附近误判 |
| C | ❌ 9 是 2^7 与 100 的差误算 |
| D ✅ | 深度 = ⌊log₂n⌋ + 1 = ⌊log₂100⌋ + 1 = 6 + 1 = **7**（2^6 ≤ 100 < 2^7） |

> 考点：完全二叉树深度公式。真题溯源。

---

**26.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | 层序 `ABCDEF`：第二层 B、C；前序 `ABDECF`：B 左 D 右 E，C 左 F。后序 = 左子树(D E B) + 右子树(F C) + 根(A) = **D E B F C A** |
| B | ❌ `D E B C F A` 把右子树后序 F C 写反 |
| C | ❌ 根 A 应在最后 |
| D | ❌ 同 B，左右子树拼接顺序错 |

> 考点：层序 + 前序推断树结构，再求后序。陷阱：层序只能确定"层"信息，需要结合前序确定左右。

---

**27.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ✅ 是树结构（目录嵌套） |
| B | ✅ 是树结构（标签嵌套） |
| C | ✅ 是树结构（家谱树） |
| D ❌ | **不是树**：地铁线路图有**环**（多条线路交叉换乘），不满足"任意两点唯一路径" |

> 考点：树的判定（连通 + 无环 + 唯一路径）。陷阱：把"网状/有环结构"误当树。

---

**28.** 答案 **A**

| 选项 | 分析 |
|------|------|
| A ✅ | 总边数 = 4×1 + 3×3 + 2×4 = 21，节点数 = 21 + 1 = 22（吻合）；叶子 = 22 - 4 - 3 - 2 = **13** |
| B | ❌ 12 是少算了一种度（漏了度为 4 的节点） |
| C | ❌ 14 是把边数当节点数算 |
| D | ❌ 11 是 22 - 4 - 3 - 4 的误算 |

> 考点：多叉树的度 → 边数 → 节点数 → 叶子数。陷阱：总边数 = Σ(度 × 节点数)，不是 Σ节点数。

---

**29.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ❌ `D G E B C H F A` 右子树后序应为 H F C，不是 C H F |
| B | ❌ `G D E` 左子树内部顺序错（应为 D G E） |
| C | ❌ `D E G` 把 E、G 顺序颠倒（G 是 E 的左孩子） |
| D ✅ | 前序定根 A；中序 `DBGE A CHF` 分左右：左 {D,B,G,E}、右 {C,H,F}。左子树 B(D, E(G)) 后序 D G E B；右子树 C(F(H)) 后序 H F C；总后序 = **D G E B H F C A** |

> 考点：已知前序 + 中序还原二叉树，求后序。真题溯源。

---

**30.** 答案 **D**

| 选项 | 分析 |
|------|------|
| A | ❌ 满二叉树的前序 ≠ 中序（除单节点外） |
| B | ❌ 全左链时中序是反序（DCBA），不会等于前序 |
| C | ❌ 右链树存在：A→B→C→D 全是右孩子时，前序 = 中序 = ABCD |
| D ✅ | 前序 = 中序说明"根总是第一个"，即每棵子树都没有左孩子 → 每个节点**都只有右孩子**（退化成右链） |

> 考点：遍历与树形的关系（退化链表）。陷阱：前序 = 中序 ⟺ 无左子树（全是右孩子）。

---

## 🧠 易错点速查

| 题号 | 考点 | 易错点 |
|:----:|------|--------|
| 1 | 边数 = 节点数 - 1 | 把节点数当边数 |
| 2 / 11 / 20 / 26 | 两种遍历还原 | 递归分治时左右子树范围切分错；前序首/后序末定根 |
| 3 | 度 → 边数 → 节点数 | 忘记"总边数 = Σ度"，忘记 +1 |
| 4 | 树的形态计数 | 卡特兰数 1、2、5…，左右子树区分 |
| 5 / 17 | 满二叉树公式 | 2^k 与 2^k-1、总节点数与叶子数混淆 |
| 6 / 23 | 完全二叉树编号 | 忘记判断 2i / 2i+1 是否 ≤ n（越界） |
| 7 | n₀ = n₂ + 1 | 联立方程时漏掉 n₁ |
| 9 | 二叉树性质 | "2^k 个" 与 "2^k - 1 个" 差 1 |
| 10 / 25 / 14 | 完全二叉树公式 | ⌈n/2⌉ 与 ⌊n/2⌋、深度 +1 漏算、最少 vs 最多节点 |
| 8 / 15 / 24 | n₀ = n₂ + 1 应用 | 求总节点时忘记加 n₁；"无度1节点"时 n = n₀ + n₂ |
| 12 | 最小深度 | 深度最小 ⟺ 尽量排满（完全二叉树形态） |
| 13 | 编号与层数 | 第 k 层编号从 2^(k-1) 开始 |
| 16 | 前序 + 后序还原 | 只有无单分支节点（满二叉树）才能唯一确定 |
| 18 | 遍历与根的位置 | 层序第一个是根、后序最后一个是根，容易记反 |
| 21 | 树 vs 二叉树 | 二叉树 ≠ 度为 2 的树（有序性） |
| 22 | 满二叉树逆推 | 总节点数 2^k - 1 与叶子数 2^(k-1) 混淆 |
| 28 | 多叉树度推叶子 | 总边数 = Σ(度×节点数)，易漏算某一种度 |
| 29 / 20 / 16 | 遍历还原 | 左右子树范围切分错；前序首/后序末定根 |
| 30 | 前序 = 中序 | 全右链（无左子树）的退化树 |

---

*难度参考：CSP-J / GESP 初赛 ｜ 2026-08-03*
